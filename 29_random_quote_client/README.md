# 29_random_quote_client

Fetch a random quote and optionally save favorites locally.

Usage:
- pip install -r requirements.txt
- python quotes.py
- python quotes.py favorites  (show saved favorites)

Possible improvement:
- Add tagging of favorites and simple search through saved quotes.
