# 30_api_response_analyzer

Analyze JSON responses (from a URL or a local file) and report element types and nesting depth.

Usage:
- pip install -r requirements.txt
- python analyzer.py https://api.example.com/data
- python analyzer.py data.json

Possible improvement:
- Generate a readable schema summary (e.g., show all keys and sample types per key).
