# 22_currency_converter

Convert currencies using exchangerate.host (free, no API key).

Usage:
- pip install -r requirements.txt
- python convert.py 100 USD EUR

Possible improvement:
- Add caching of recent rates to avoid repeated API calls.
