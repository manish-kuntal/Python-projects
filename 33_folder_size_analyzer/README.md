# 33_folder_size_analyzer

Calculate total folder size and list top largest files.

Usage:
- python size_analyzer.py /path/to/folder --top 20

Possible improvement:
- Add per-folder breakdown and a nicer table output (tabulate).
