# 36_image_metadata_viewer

Display image format, dimensions, and EXIF metadata using Pillow.

Usage:
- pip install -r requirements.txt
- python image_meta.py photo.jpg

Possible improvement:
- Pretty-print GPS coordinates from EXIF and offer a link to view location on a map.
