# 27_url_status_checker

Check the HTTP status and response time for multiple URLs.

Usage:
- pip install -r requirements.txt
- python checker.py https://example.com https://github.com

Possible improvement:
- Add concurrency (threads or asyncio) to check many URLs faster.
