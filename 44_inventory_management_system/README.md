# 44_inventory_management_system

Manage simple product inventory with low-stock detection.

How to use:
- Run inventory.py for a demo. Use functions in scripts to build CLI commands.

Improvement suggestion:
- Add CSV import/export and alerting when stock is low (email or webhook).
