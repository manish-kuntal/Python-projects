# 50_command_line_assistant

A small assistant that dispatches to other project scripts in this repo (calculator, notes, tasks).

Usage:
- python assistant.py calc
- python assistant.py notes list
- python assistant.py tasks add "Buy milk"

Improvement suggestion:
- Add a plugin system so third-party scripts can be registered and discovered.
