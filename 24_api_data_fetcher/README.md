# 24_api_data_fetcher

Simple reusable API fetcher that retrieves JSON from a URL and pretty-prints it.

Usage:
- pip install -r requirements.txt
- python fetcher.py https://api.example.com/data

Possible improvement:
- Add output validation against a JSON Schema.
