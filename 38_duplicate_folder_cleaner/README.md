# 38_duplicate_folder_cleaner

Scan a folder for duplicate files and write a JSON report listing groups of duplicates.

Usage:
- python duplicate_folder_cleaner.py /path/to/search --out dup_report.json

Improvement suggestion:
- Add an interactive review tool to select which duplicates to delete or move to quarantine.
