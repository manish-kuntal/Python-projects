# 35_pdf_information_extractor

Extract basic metadata and a short text sample from a PDF using `pypdf`.

Usage:
- pip install -r requirements.txt
- python pdf_info.py document.pdf

Possible improvement:
- Add extraction of all text and save as plain .txt, plus better handling of scanned PDFs (OCR).
