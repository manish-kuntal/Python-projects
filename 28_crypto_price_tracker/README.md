# 28_crypto_price_tracker

Fetch cryptocurrency prices from CoinGecko (no API key required).

Usage:
- pip install -r requirements.txt
- python crypto.py bitcoin ethereum dogecoin

Possible improvement:
- Add periodic polling and simple change alerts or save history to CSV.
