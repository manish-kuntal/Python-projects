# 48_data_visualization_project

Load a CSV dataset, compute basic statistics, and generate simple histograms for numeric columns.

Usage:
- pip install -r requirements.txt
- python visualize.py data.csv

Improvement suggestion:
- Add interactive charts (plotly) and better cleaning (dropna, imputation).
