# 18_file_organizer

Organize files in a folder into subfolders by extension (Images, Documents, Archives, etc.).

Usage:
- python organize.py /path/to/folder
- python organize.py /path --dry  (show actions without moving)

Improvement suggestion:
- Allow custom extension-to-folder mappings via a config file.
